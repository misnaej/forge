bump: minor

Agent model choices now include `fable` tier above `opus`, available via user approval only. See `agents/_TEMPLATE.md` "Model per role" for the governing rule: upgrading past `opus` is the user's call, never the agent's.
